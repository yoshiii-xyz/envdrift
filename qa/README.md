# Private QA evidence

Store iteration records here. This directory is ignored by Git. Each record
must contain the toolchain, exact commands, exit codes, test counts, package
result, fuzz bounds, hosted run identifiers, and known validation limits.

Never store credentials, registry login files, private keys, or unredacted
external paths.
